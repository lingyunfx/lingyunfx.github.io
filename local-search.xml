<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Nuke开发系列之节点操作</title>
    <link href="/2020/10/30/nuke-nodes-intro/"/>
    <url>/2020/10/30/nuke-nodes-intro/</url>
    
    <content type="html"><![CDATA[<h1 id="Nuke开发相关的文档"><a href="#Nuke开发相关的文档" class="headerlink" title="Nuke开发相关的文档"></a>Nuke开发相关的文档</h1><p><a href="https://support.foundry.com/hc/zh-cn">Nuke官方技术支持论坛</a><br><a href="https://learn.foundry.com/nuke/developers/11.2/pythondevguide/">Python开发文档</a><br><a href="https://learn.foundry.com/nuke/developers/11.2/pythonreference/">Python API文档</a><br><a href="https://www.yiibai.com/tcl">Nuke TCL教程</a></p><h1 id="Nuke节点操作"><a href="#Nuke节点操作" class="headerlink" title="Nuke节点操作"></a>Nuke节点操作</h1><h3 id="获取节点"><a href="#获取节点" class="headerlink" title="获取节点"></a>获取节点</h3><pre><code class="hljs python"><span class="hljs-keyword">import</span> nuke<span class="hljs-comment"># 已知节点的名称，使用toNode函数获取，返回一个节点对象。</span>nuke.toNode(<span class="hljs-string">&#x27;Text1&#x27;</span>)<span class="hljs-comment"># 获取当前选择的单个节点，返回一个节点对象。</span>nuke.selectedNode()<span class="hljs-comment"># 获取当前选择的多个节点，返回一个列表。</span>nuke.selectedNodes()<span class="hljs-comment"># 获取工程所有节点</span>all_nodes = nuke.allNodes()<span class="hljs-comment"># 获取工程所有指定类型的节点</span>all_dot_nodes = nuke.allNodes(<span class="hljs-string">&quot;Dot&quot;</span>)</code></pre><h3 id="获取，设置节点属性"><a href="#获取，设置节点属性" class="headerlink" title="获取，设置节点属性"></a>获取，设置节点属性</h3><pre><code class="hljs python"><span class="hljs-comment"># 获取cliptype的值</span>t = nuke.toNode(<span class="hljs-string">&#x27;Text1&#x27;</span>)<span class="hljs-built_in">print</span> t[<span class="hljs-string">&#x27;cliptype&#x27;</span>].value()<span class="hljs-meta">&gt;&gt;&gt; </span><span class="hljs-built_in">format</span><span class="hljs-comment"># 改变cliptype的值</span>t[<span class="hljs-string">&#x27;cliptype&#x27;</span>].setValue(<span class="hljs-string">&#x27;bbox&#x27;</span>)<span class="hljs-comment"># 因为这个值是一个下拉菜单，所以可以传index参数</span>t[<span class="hljs-string">&#x27;cliptype&#x27;</span>].setValue(<span class="hljs-number">4</span>)<span class="hljs-comment"># 设置Read文件路径,并自动set帧范围(对帧序列无效)</span>read_node[<span class="hljs-string">&#x27;file&#x27;</span>].fromUserText(video_file)</code></pre><h3 id="创建和删除节点"><a href="#创建和删除节点" class="headerlink" title="创建和删除节点"></a>创建和删除节点</h3><pre><code class="hljs python"><span class="hljs-comment"># 这种方式适合nuke界面打开的时候，代码执行后会跳转到节点的属性编辑窗。</span>nuke.createNode(<span class="hljs-string">&#x27;Write&#x27;</span>)<span class="hljs-comment"># 这种方法适合没有打开nuke，在后台的一些操作。</span>nuke.nodes.Write()<span class="hljs-comment"># 创建节点，忽略弹出属性窗口</span>nuke.createNode(<span class="hljs-string">&quot;Blur&quot;</span>, inpanel=<span class="hljs-literal">False</span>)<span class="hljs-comment"># 创建节点的时候，设置节点属性</span>nuke.nodes.Dot(xpos=<span class="hljs-number">100</span>, ypos=<span class="hljs-number">100</span>)<span class="hljs-comment"># 删除节点</span>nuke.delete(node)<span class="hljs-comment"># 删除工程指定类型的节点</span><span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> nuke.allNodes(<span class="hljs-string">&quot;Dot&quot;</span>):  nuke.delete(i)</code></pre><h3 id="连接节点"><a href="#连接节点" class="headerlink" title="连接节点"></a>连接节点</h3><pre><code class="hljs python">t = nuke.toNode(<span class="hljs-string">&#x27;Text1&#x27;</span>)w = nuke.toNode(<span class="hljs-string">&#x27;Write1&#x27;</span>)<span class="hljs-comment"># setInput的第一个参数是输入编号</span><span class="hljs-comment"># 第二个参数是需要连接的上游节点</span>w.setInput(<span class="hljs-number">0</span>, t)<span class="hljs-comment"># 得到节点的input关系,返回节点对象</span>w.<span class="hljs-built_in">input</span>(<span class="hljs-number">0</span>)</code></pre><h3 id="root操作"><a href="#root操作" class="headerlink" title="root操作"></a>root操作</h3><pre><code class="hljs python"><span class="hljs-comment"># 获取nuke文件路径</span>nuke.root().name()<span class="hljs-comment"># 获取项目设置的起始帧</span>nuke.root().firstFrame()</code></pre><h3 id="使一个节点被选中"><a href="#使一个节点被选中" class="headerlink" title="使一个节点被选中"></a>使一个节点被选中</h3><pre><code class="hljs python"><span class="hljs-comment"># 使一个节点被选择状态</span>node = nuke.toNode(<span class="hljs-string">&quot;Grade1&quot;</span>)node.setSelected(<span class="hljs-literal">True</span>)</code></pre><h3 id="渲染指定节点"><a href="#渲染指定节点" class="headerlink" title="渲染指定节点"></a>渲染指定节点</h3><pre><code class="hljs python"><span class="hljs-comment"># 参数分别是Write节点名，渲染起始帧，渲染结束帧，步长。</span>nuke.execute(<span class="hljs-string">&#x27;Write1&#x27;</span>, <span class="hljs-number">1001</span>, <span class="hljs-number">1010</span>, <span class="hljs-number">1</span>)</code></pre><h3 id="获取设置节点位置"><a href="#获取设置节点位置" class="headerlink" title="获取设置节点位置"></a>获取设置节点位置</h3><pre><code class="hljs python">node = nuke.toNode(<span class="hljs-string">&quot;Grade1&quot;</span>)<span class="hljs-built_in">print</span> node.xpos()<span class="hljs-built_in">print</span> node.ypos()node.setXpos(<span class="hljs-number">0</span>)node.setYpos(<span class="hljs-number">20</span>)node.setXYpos(<span class="hljs-number">0</span>, <span class="hljs-number">20</span>)</code></pre><h3 id="复制粘贴节点"><a href="#复制粘贴节点" class="headerlink" title="复制粘贴节点"></a>复制粘贴节点</h3><pre><code class="hljs python"><span class="hljs-comment"># 将选择的节点复制到粘贴板</span>nuke.nodeCopy(<span class="hljs-string">&quot;%clipboard%&quot;</span>)<span class="hljs-comment"># 将粘贴板的内容，粘贴到文件中</span>nuke.nodePaste(<span class="hljs-string">&quot;%clipboard%&quot;</span>)</code></pre><h1 id="动画操作"><a href="#动画操作" class="headerlink" title="动画操作"></a>动画操作</h1><pre><code class="hljs python"><span class="hljs-comment"># 设置为可key帧状态</span>node = nuke.toNode(<span class="hljs-string">&quot;Blur1&quot;</span>)knob = node(<span class="hljs-string">&quot;size&quot;</span>)knob.setAnimated()<span class="hljs-comment"># 给指定帧数key帧，第一个参数是数值，第二个参数是帧数</span>knob.setValueAt(<span class="hljs-number">55</span>, <span class="hljs-number">10</span>)<span class="hljs-comment"># 比如size属性，有x和y两个值的时候，使用第三个参数来控制</span>knob.setValueAt(<span class="hljs-number">77</span>, <span class="hljs-number">100</span>, <span class="hljs-number">0</span>)knob.setValueAt(<span class="hljs-number">88</span>, <span class="hljs-number">100</span>, <span class="hljs-number">1</span>)<span class="hljs-comment"># 判断一个属性是否有关键帧</span><span class="hljs-built_in">print</span> knob.isAnimated()<span class="hljs-comment"># 获取key帧动画</span><span class="hljs-built_in">print</span> knob.animations()<span class="hljs-comment"># 只获取size属性中，h的动画</span><span class="hljs-built_in">print</span> knob.animations(<span class="hljs-number">1</span>)<span class="hljs-comment"># 获取size属性中，h的每一个关键帧和对应的数值</span>animation = knob.animation(<span class="hljs-number">1</span>)<span class="hljs-keyword">for</span> key <span class="hljs-keyword">in</span> animation.keys():  <span class="hljs-built_in">print</span> key.x, key.y          <span class="hljs-comment"># x为关键帧， y为对应的数值</span><span class="hljs-comment"># 获取非关键帧的数值</span><span class="hljs-built_in">print</span> animation.evaluate(<span class="hljs-number">60</span>)<span class="hljs-comment"># 拷贝动画</span>nuke.toNode(<span class="hljs-string">&quot;Grade1&quot;</span>)[<span class="hljs-string">&quot;white&quot;</span>].copyAnimations(knob.animations())<span class="hljs-comment"># 删除关键帧</span><span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> knob.animations():  i.clear()  knob.deleteAnimation(i)</code></pre><h1 id="文件操作"><a href="#文件操作" class="headerlink" title="文件操作"></a>文件操作</h1><pre><code class="hljs python"><span class="hljs-comment"># 打开一个nuke文件</span>nuke.scriptOpen(<span class="hljs-string">&quot;$nuke_file_path&quot;</span>)<span class="hljs-comment"># 打开一个新的空文件</span>nuke.scriptNew()<span class="hljs-comment"># 保存文件，如果括号里没有参数，则相当于ctrl+s</span>nuke.scriptSave(<span class="hljs-string">&quot;$nuke_file_path&quot;</span>)<span class="hljs-comment"># 另存为</span>nuke.scriptSaveAs(<span class="hljs-string">&quot;$nuke_file_path&quot;</span>)<span class="hljs-comment"># 关闭当前文件，回归到空文件状态</span>nuke.scriptClose()<span class="hljs-comment"># 将选择的节点，输出为一个nk文件</span>nuke.nodeCopy(<span class="hljs-string">&quot;$nuke_file_path&quot;</span>)<span class="hljs-comment"># 将一个文件导入到当前工程</span>nuke.nodePaste(<span class="hljs-string">&quot;$nuke_file_path&quot;</span>)</code></pre>]]></content>
    
    
    <categories>
      
      <category>DCC</category>
      
      <category>Nuke</category>
      
    </categories>
    
    
  </entry>
  
  
  
  
</search>
